# Mimari — {{PROJECT_NAME}}

_Bu dosya sıfırdan doldurulacak. Bootstrap sadece iskeleti kopyalar. Proje geliştikçe güncellenir._

## Veri Erişim
(ör. "Rapor datası SP, metadata CRUD EF Core" / "Tek stratejik ORM: Prisma" / "DynamoDB single-table")

## API / Service Yüzeyi
(ör. "REST Minimal API + server-rendered view" / "gRPC + tRPC" / "GraphQL")

## Auth / Authorization
(ör. "Cookie auth, role check UserRole junction" / "JWT + refresh, RBAC" / "OAuth2 + OIDC")

## Frontend
(ör. "Vanilla JS IIFE + Tailwind CDN" / "React + Vite + TanStack Query" / "Next.js App Router")

## Observability
(ör. "Serilog + Seq" / "OpenTelemetry + Grafana" / "Sentry + console")

## Bilinen Tutarsızlıklar
_Fark edildikçe eklenir. Büyük olanlar ADR'ye terfi._

### YÜKSEK risk
(yok — proje başı)

### ORTA risk
(yok)

### DÜŞÜK risk
(yok)

## Proje Durumu (snapshot)
- Olgunluk: %?
- Uncommitted: ?
- Aktif modüller:

## Referanslar
- ADR'ler: `docs/ADR/` (yazılacak).
- Bağlam: `docs/CONTEXT_MANAGEMENT.md`.
- TODO: `TODO.md`.
