# Oturum Gunlugu — 2026-06-04

## Ana Konu
Eğitim hazırlığı + dağıtım. Sunum üretimi (v1→v3, BKM şablonuna aktarım), bkm isim temizliği (log dahil), presentation-builder + bkm-sunum skill'leri, deploy paketleyici. Tüm birikmiş iş 7 bucket'a commit'lendi + v1.2.0 tag.

## Tamamlananlar

### Sunum / eğitim materyali
- Eğitim sunumu 3 sürüm: v1 (emoji düz), v2 (sandwich+gölge), v3 (Lucide ikon + donut/bar/3D stack). PDF + v3 pptx.
- v3 BKM Kitap şablonuna aktarıldı (`Desktop\Sunum_Egitim.pptx`): kırmızı ikon-desenli bantlar + bkmkitap logo. KRİTİK keşif: branding slayt arka planında (`<p:bg>` blip), layout/master'da değil → orijinal slayt bg'si yeni slaytlara kopyalanmalı.
- BKM kırmızısı #E30622 palet, navy/teal kaldırıldı.
- Görsel QA artık PowerPoint COM ile (soffice/LibreOffice yok) — gerçekten bakılıyor.

### Skill'ler
- `templates/.claude/skills/presentation-builder/` — generic deck üretimi (HTML→PDF, anti-slop, Lucide, infografik, görsel QA). Bootstrap ile dağıtılır.
- `~/.claude/skills/bkm-sunum/` — KULLANICI seviyesi (BKM'ye özel, paylaşılmaz). "BKM için yap" tetikler. assets/: çalışan jeneratör + beyaz/kırmızı ikon setleri.

### bkm/belinza/yonetiq temizliği
- Dağıtılan her şey temiz. CHANGELOG isimsizleştirildi, journal (2026-06-03) scrub edildi, TUM_PROJE_KODLARI.txt + kodlari_topla_v2.bat gitignore'landı.

### Deploy
- `bin/package.py` — allowlist paketleyici. `python bin/package.py [--training]` → dist/ altında temiz zip'ler (template 0.2MB / eğitim kiti 1.9MB). node_modules/sunum/journal/_icons/junk sızmaz.
- `docs/DEPLOY.md` — devir rehberi.

### Commit + sürüm
- 7 commit (A: bkm-fix, B: kurallar+bootstrap, C: presentation-builder, D: deploy, E: docs, F: training, chore: gitignore).
- Tag: **v1.2.0**.

## Build / Test Durumu
- package.py test: 89 dosya / 0.2MB template + 10 dosya / 1.9MB eğitim kiti. Sızıntı yok.
- bkm-sunum asset script: yeni konumdan 16 slayt üretti.
- BKM pptx: PowerPoint COM ile 6 slayt görsel doğrulandı (kapak/divider/stack/donut/tablo) — markalı, çakışma yok.

## Commit Durumu
- Bu oturum: 7 commit + v1.2.0 tag.
- Kalan uncommitted: `templates/.claude/settings.json` (stray — template .tmpl kullanıyor, kasıtlı bırakıldı).
- Push YAPILMADI (kullanıcı istemedi).

## Yarim Kalan / Yarin'a
1. **Eğitim günü:** `python bin/package.py --training` → 2 zip'i dağıt. README + LEADER_PLAYBOOK ile başlat.
2. Dağıtmadan önce istenirse `git push` + push tag (`git push --tags`).
3. `templates/.claude/settings.json` stray dosya — sil veya .tmpl ile değiştir (karar ver).
4. Opsiyonel: diğer 4 ortam PDF'ine de acemi gibi çok-ekranlı storyboard.

## Kararlar
- BKM'ye özel sunum skill'i KULLANICI seviyesinde (~/.claude) — template'e GİRMEZ, paylaşılmaz.
- presentation-builder GENERIC, template'e girer (her projeye gider).
- Deploy allowlist-tabanlı: kişisel dosya eklense bile pakete sızmaz.
- Sunum görsel QA = PowerPoint COM (Windows'ta soffice yok).

## Dikkat
- BKM branding = slayt background image. Yeni slayt eklerken copy_bg ZORUNLU yoksa boş beyaz.
- LF→CRLF uyarıları normal; .gitattributes yok.
- Eğitim PDF'leri "© Fikri Eren 2026" imzalı.

## Yarina Baslangic Noktasi
1. VERSION/CHANGELOG kontrol → `python bin/package.py --training`.
2. dist/ iki zip + (istenirse) git push --tags.
3. settings.json stray kararı.

## Yardımcı Dosyalar
### Yeni
- `bin/package.py`, `bin/update-all.sh`, `bin/projects.txt`, `CHANGELOG.md`, `VERSION`
- `docs/{DEPLOY,LEADER_PLAYBOOK,GELISTIRICI_REHBERI,PROJE_OZEL_*}.md`
- `docs/{acemi,seviye,desktop,chat,cowork,sunum}/` — eğitim materyali
- `templates/.claude/skills/presentation-builder/`, `~/.claude/skills/bkm-sunum/`
- `templates/.claude/rules/_universal/{agent-usage,error-handling,plan-first,test-discipline,todo-verification}.md`
### Değişen
- bootstrap.ps1/.sh (14 kural), README, generic'leşen template dosyaları, journal scrub

---

## Oturum 2 — Afiş + workflow kullanımı

### Tamamlananlar
- **Kullanım afişi:** `docs/afis/Claude_Kullanim_Afisi.pdf` — tek sayfa A4, duvara asılık. Lucide ikon, BKM kırmızısı, "© Fikri Eren 2026". İçerik: "Claude hatırlamaz" kavramı + günlük ritüel şeridi + "ne dersen ne olur" tablosu + ustalık yolu (L0-L4) + uyarı/takılırsan kutuları. Generator: `docs/afis/build_afis.py` (playwright HTML→PDF). Commit: f853d4f.
- **Dynamic Workflows sorusu:** Anthropic blog (claude.com/blog/introducing-dynamic-workflows) açıklandı — bu ortamda Workflow aracı zaten mevcut. Tetik: "workflow oluştur"/"use a workflow" veya ultracode aç. Onlarca-yüzlerce paralel subagent + adversarial verify. Senin projelerine örnekler verildi (operax SP audit, reporthub güvenlik, template çok-proje kural denetimi). Token uyarısı: scoped başla.

### Commit Durumu
- Bu oturum: 1 commit (afiş) + bu handoff.
- Kalan: `templates/.claude/settings.json` (stray, hâlâ kasıtlı bırakıldı).
- Push YAPILMADI.

### Yarim Kalan / Yarin'a
1. **Eğitim günü:** `python bin/package.py --training` → 2 zip dağıt. Afiş PDF'i de bas/as.
2. İstenirse workflow ile gerçek scoped audit demosu (operax SP veya reporthub güvenlik).
3. settings.json stray kararı (sil/tmpl).
4. İstenirse `git push --tags`.

### Öneriler (yapılmadı, beklemede)
- Afiş A3 büyük poster varyantı / tam BKM bant+logo'lu versiyon.
- presentation-builder'a "afiş/broşür modu" ekleme.
- Template'e `workflow-patterns.md` (ekip ne zaman/nasıl workflow kullanır).
