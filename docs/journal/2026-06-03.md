# Oturum Gunlugu — 2026-06-03

## Ana Konu

Template'i multi-project (bkm/belinza/yonetiq) hardcode'undan arındırıp tamamen generic hale getirdik. Projelerden (operax, reporthub, fifo) generic-değerli kuralları hasat ettik. Yazılımcılara verilecek 4 katmanlı dökümantasyon seti yazıldı: katalog → kavram → somut senaryo → teknik yazım.

## Tamamlananlar

### 1. bkm/belinza/yonetiq → generic temizlik
- Silinen 5 stray root rule (bootstrap kullanmıyordu, bkm-bulaşmış): `templates/.claude/rules/{commit-discipline,plan-first,session-memory,session-protocol,sql-server-conventions}.md`
- `templates/.claude/skills/session-handoff/SKILL.md` — multi-project journal yapısı kaldırıldı, tek `docs/journal/YYYY-MM-DD.md`
- `templates/.claude/agents/commit-splitter.md` — proje bucket → feature/katman bucket
- `templates/.claude/hooks/session-start.sh` — proje döngüsü kaldırıldı, tek son journal
- `templates/.claude/hooks/post-commit-journal.sh` — scope→proje tespiti kaldırıldı
- `templates/.claude/rules/_universal/plan-first.md` — `feat(bkm):` → `feat:`, bkm örnekleri generic
- `templates/plans/feature-template.md`, `plans/README.md` — proje satırı/scope kaldırıldı
- `templates/docs/ADR/003-plan-first-tier-system.md` — crossproject referansları temizlendi
- Doğrulandı: `grep -rinE 'bkm|belinza|yonetiq|...'` → TEMIZ

### 2. Generic kural hasadı (projelerden)
- Bootstrap BUG'ı bulundu+düzeltildi: `_universal/`'de 10 kural vardı ama bootstrap sadece 4'ünü kopyalıyordu. Eksikler (coding-discipline, response-style, before-major-change, file-size-discipline, plan-first) artık kopyalanıyor.
- 4 YENİ generic universal kural (operax/reporthub'dan damıtıldı, proje-bağımsız):
  - `_universal/todo-verification.md` — TODO iddiasını canlı kodla doğrula
  - `_universal/test-discipline.md` — "bitti" demeden test çalıştır, build≠test
  - `_universal/agent-usage.md` — subagent delegasyon + model katmanlama (haiku/sonnet/opus)
  - `_universal/error-handling.md` — beklenen sonuç vs gerçek exception
- `bin/bootstrap.ps1` + `bin/bootstrap.sh` — UNIVERSAL_FILES listesi 4 → 14
- `README.md` — evrensel kural listesi güncellendi
- Test: izole klasöre bootstrap çalıştı, 14 kural + stack temiz kopyalandı

### 3. Yazılımcı dökümantasyon seti (4 yeni doc, docs/)
- `GELISTIRICI_REHBERI.md` — ne var, nasıl tetiklerim (skill/agent/komut/hook/kural kataloğu + karar tablosu)
- `PROJE_OZEL_KAVRAM.md` — neden/niçin var, ne zaman doğar (zihinsel model, hook derinlemesine)
- `PROJE_OZEL_ORNEK_SENARYO.md` — somut ERP/fatura skill örneği, kopyala-yapıştır prompt, önce/sonra
- `PROJE_OZEL_OLUSTURMA.md` — teknik yazım (frontmatter, örnek katalog, model tiering)
- 4 doc + README çapraz bağlandı

## Build / Test Durumu
- Build: yok (markdown + script repo)
- bootstrap.sh izole test: yeşil (14 universal kural kopyalandı, proje adı kalıntısı yok)
- Smoke: bootstrap dry-run /tmp'de çalıştı, temizlendi

## Commit Durumu
- Uncommitted: 22 (eşik 15 aşıldı — commit-split önerilir)
- Bu oturumda commit YOK (kullanıcı istemedi)
- Benim iş: 10 modified + ~9 yeni dosya (4 doc + 4 rule + journal)
- İlgisiz untracked (önceki oturumdan, dokunma): `TUM_PROJE_KODLARI.txt`, `kodlari_topla_v2.bat`, `templates/.claude/settings.json`

## Yarim Kalan / Yarin'a

### Üst öncelik
1. **Commit** — bu oturumun işi 3 mantıksal bucket'a bölünüp commit'lenmeli (commit-splitter):
   - `fix(template): bkm/belinza/yonetiq hardcode kaldırıldı, generic hale getirildi`
   - `feat(template): 4 yeni universal kural + bootstrap tüm kuralları kopyalıyor`
   - `docs: yazılımcı rehber seti (4 doc) eklendi`

### Düşük öncelik
2. `templates/.claude/settings.json` (untracked) — bu template repo'sunun kendi settings'i mi, yoksa yanlışlıkla mı eklendi? Kontrol et — template `settings.json.tmpl` kullanıyor, plain `settings.json` gereksiz olabilir.
3. `TUM_PROJE_KODLARI.txt` + `kodlari_topla_v2.bat` — repo köküne ait değil gibi, .gitignore'a eklenmeli veya silinmeli.

## Kararlar

### Reddedilenler
- ~~Stack/domain-özel skill'leri (mali-evrak-mevzuat, local-llm-integration, pgsql-porter, sql-sp-reviewer) template'e kopyala~~ — generic değil. Bunun yerine PROJE_OZEL dökümanlarında **pattern** olarak örneklendi.
- ~~phase-review-gate kuralını universal yap~~ — proje agent setine sıkı bağlı. Generic anlatımı PROJE_OZEL_OLUSTURMA §6'da.

### Seçilenler
- Multi-project repo varsayımı tamamen terk edildi — d:\dev altındaki projeler (reporthub, fifo, sqlcli, operax, sqlserver-mcp-server, claude-context-template) **ayrı tekil repo'lar**, monorepo değil. Journal = tek `docs/journal/YYYY-MM-DD.md`.

## Dikkat Edilmesi Gerekenler

- `_universal/plan-first.md` git'te untracked görünüyor — daha önce tracked sanıyordum; commit'lerken dahil et.
- Stray root rule silmeleri git'e `D` olarak yansımıyor (hiç commit'lenmemişlerdi, untracked'di) — sessiz silindi, sorun yok.
- LF→CRLF uyarıları normal (Windows), .gitattributes yoksa eklenebilir.

## Yarina Baslangic Noktasi

1. `git status` — 22 dosyayı 3 bucket'a böl (yukarıdaki commit mesajları).
2. commit-splitter agent çağır veya elle stage+commit.
3. settings.json + TUM_PROJE_KODLARI.txt durumunu netleştir (sil/gitignore/commit?).

## Yardımcı Dosyalar

### Yeni
- `docs/GELISTIRICI_REHBERI.md`, `docs/PROJE_OZEL_KAVRAM.md`, `docs/PROJE_OZEL_ORNEK_SENARYO.md`, `docs/PROJE_OZEL_OLUSTURMA.md`
- `templates/.claude/rules/_universal/{todo-verification,test-discipline,agent-usage,error-handling}.md`

### Değişen
- `bin/bootstrap.ps1`, `bin/bootstrap.sh` — UNIVERSAL_FILES 14 dosya
- `README.md` — kural listesi + doc linkleri
- `templates/.claude/{skills/session-handoff/SKILL.md, agents/commit-splitter.md, hooks/session-start.sh, hooks/post-commit-journal.sh}` — generic
- `templates/{plans/feature-template.md, plans/README.md, docs/ADR/003-*.md}` — generic

### Silinen
- `templates/.claude/rules/{commit-discipline,plan-first,session-memory,session-protocol,sql-server-conventions}.md` (stray, bkm-bulaşık)
