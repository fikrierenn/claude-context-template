# DEVİR — şablonu geliştirme-zamanı çatısına dönüştürme (2026-09-23)

> **Bu belgeyi açan oturum için yazıldı. Tek başına yeterlidir — başka bir oturumun hafızasına
> ihtiyaç duymaz.** Kaynağı `bkm-magaza` oturumudur (sunucu SANAL-FIKRI, 23.09.2026). Orada bir
> kural kapısı kurulurken bu ekosistem sorununa çarpıldı, 39 depo ölçüldü, karar yazıldı.
>
> **Okuma sırası:** (1) bu belge · (2) `docs/MIMARI-KARARI-2026-09-23.md` (karar + reddedilenler) ·
> (3) `CHANGELOG.md` 1.4.0 (önceki teşhis, eksik kalan yanıyla birlikte).

---

## 0. Bir cümlede

Şablonun **mimarisi doğru** (`_universal`/`stacks`/`project` ayrımı, bootstrap, harvest),
**tüketim modeli yanlış**: dosyaları KOPYALIYOR. Kopya doğduğu anda ikinci bir gerçek olur.
39 depoda **158 sapmış kural** var. Sapması sıfır olan tek depo, kopyalamayan depo.
**Karar: kopya → referans.** Şablon, Solum'un runtime için olduğu şeyin geliştirme-zamanı karşılığı olur.

---

## 1. NASIL BURAYA GELİNDİ (bağlam — atlanmasın)

`bkm-magaza` oturumunda GMY şunu sordu: *"kodlama kurallarımız burada geçerli değil mi bir bak."*
Denetim yapıldı, **`file-size-discipline` ihlali** çıktı (4 dosya 500 satır kırmızı çizgisinin
üstünde). Sebep kural değil **kapısızlıktı** — commit kapısı boyuta hiç bakmıyordu.

Sonra GMY *"yeni yazılanlar artık Türkçe olmamalı"* dedi. Kural (`turkish-ui.md` § Dil Ayrımı:
kod İngilizce) zaten vardı ve **hiç uygulanmamıştı**. Kapı yazıldı.

Sonra GMY *"repoların birinde böyle bir kapı yapmıştım sanki"* dedi. **Arandı ve bulundu:**
`pusula/tools/turkce_tanimlayici_denetimi.py` — benim yazdığımdan çok daha olgun. Kendi kapım
silindi, olgun olan bağlandı. (`footprint-ladder`: *"yeni kapı yaratmadan önce mevcudu kontrol et."*)

Sonra GMY *"bu tarz tool'ları ortak generik bir yapıya mı döndürmek gerek"* diye sordu. Ölçüldü:
kuralların **11-14 kopyası** var ve içerikleri farklı. Sonra *"template adında bir şeyler kurmaya
çalışmıştım"* dedi — **bu depo bulundu.** Sonra *"solum gibi orası da skill/hook framework'ü
olmalı"* ve *"tüm projeleri tara, template tarafını geliştir."*

📐 **Dersin kendisi bu:** üç kez üst üste, "yeni bir şey kurmadan önce var olanı ara" kuralı
işletilmeseydi üç ayrı kopya doğacaktı. Bu oturum da aynı disiplinle çalışmalı.

---

## 2. TABAN ÖLÇÜM — 39 depo, 2026-09-23

Tekrar koşumu: `bash bin/durum.sh` (bu işle birlikte eklendi; ~90 satır, çıkış hep 0, rapor aracı).

```
Sablon : claude-context-template (v1.4.0) · _universal 18 kural
TOPLAM : 39 depo · aynı 41 · SAPMA 158 · yerel 139 · referans modeli 1 depo
```

| depo | aynı | sapma | yerel | toplam | model |
|---|--:|--:|--:|--:|---|
| ajan | 1 | **16** | 16 | 33 | KOPYA |
| Operax | 1 | **15** | 10 | 26 | KOPYA |
| hal | 0 | **14** | 10 | 24 | KOPYA |
| BkmArgus | 3 | **13** | 8 | 24 | KOPYA |
| yonbkm | 4 | **13** | 7 | 24 | KOPYA |
| spendmev3 | 0 | **13** | 4 | 17 | KOPYA |
| **pusula** | 3 | **12** | 7 | 22 | KOPYA |
| reporthub | 1 | **12** | 11 | 24 | KOPYA |
| mizan | 1 | **12** | 2 | 15 | KOPYA |
| ChocoTR | 1 | 6 | 6 | 13 | KOPYA |
| fifo | 0 | 5 | 11 | 16 | KOPYA |
| MIMBAL | 0 | 5 | 1 | 6 | KOPYA |
| pivoterahub | 3 | 5 | 3 | 11 | KOPYA |
| FrameOS | 0 | 4 | 3 | 7 | KOPYA |
| ENS | 0 | 3 | 1 | 4 | KOPYA |
| sqlserver-mcp | 10 | 3 | 0 | 13 | KOPYA |
| claudeegitim | 0 | 2 | 0 | 2 | KOPYA |
| suite | 0 | 2 | 0 | 2 | KOPYA |
| AtlasOPS | 13 | 1 | 6 | 20 | KOPYA |
| Solum | 0 | 1 | 12 | 13 | KOPYA |
| bel | 0 | 1 | 14 | 15 | KOPYA |
| **bkm-magaza** | 0 | **0** | 1 | 1 | **REFERANS** |
| odak | 0 | 0 | 5 | 5 | — |
| Bkm-Toolbox | 0 | 0 | 1 | 1 | — |
| (14 depo daha) | 0 | 0 | 0 | 0 | — |

**Tek kuralın hâli:** `turkish-ui.md` → **11 depoda, 10 FARKLI içerik**. Sapma derinliği:
AtlasOPS 2 satır · ChocoTR 53 · ajan 111 · BkmArgus/Operax/hal **123** (şablon 55 satır, onlar 68)
— yani ayar farkı değil, **farklı kuşaklar aynı anda canlı**.

Diğerleri: `security-principles` 14 kopya · `coding-discipline` 13 · `error-handling` 12 ·
`test-discipline` 10 · `file-size-discipline` 7.

⚠ Bu sayılar **ilerleme göstergesidir**. İş bitince: `SAPMA → 0`, `referans modeli → 39`.

---

## 3. TEŞHİS — mimari değil, TÜKETİM MODELİ

`CHANGELOG 1.4.0` (23.08.2026) şunu yazmıştı:

> *"Şablon 11 Haziran'dan 23 Ağustos'a kadar 0 commit aldı. Aynı dönemde projeler `.claude/`
> altına 100'ün üzerinde commit attı (pusula 48, Operax 35, reporthub 28, BkmArgus 6). Sebep
> mimari değildi… Sebep **geri akış yolunun olmamasıydı**."*

Teşhis doğruydu ama **eksikti**: geri akış (`harvest.sh`) eklendi, **bir ay sonra yakınsama yok**.
Demek ki eksik ayak o değildi. Bir basamak daha derinde:

> **Kopya modelinde sapma hem kaçınılmaz hem görünmezdir.** Kimse 158 dosyayı elle diff'lemez →
> `update-all` çalıştırılmaz → merkez geri kalır → geri kalınca çalıştırmak *daha da* riskli olur
> (projeleri geriye alır) → kimse çalıştırmaz. Kendini besleyen döngü.

**Karşı kanıt aynı ölçümde:** sapması sıfır olan tek depo `bkm-magaza`, çünkü kopyalamıyor.
`CLAUDE.md`'sinin ilk satırları:

> *"**ANA ŞEMA VE KURALLAR BU DEPODA DEĞİL.** Hepsi komşu depodadır: `D:\Dev\pusula`. Burada
> yalnız ürün kodu yaşar. Aynı bilgi iki yerde durmaz — kopyalamak yerine işaret ediyoruz
> (kopyalanan kural bayatlar ve iki gerçek doğar)."*

ve *"Oturum başı — ZORUNLU"* bölümünde hangi kuralların okunacağını **sayıyor**. Tek yerel kuralı
`capacitor-kabuk.md` ve gerekçesi dosyanın başında yazılı: *"Bu kural yerel, çünkü komşu depo
.NET/SQL odaklı; mobil paketleme orada yok."*

**Bu desen kanıtlanmış referans uygulamadır. Göçün hedefi budur.**

---

## 4. İKİNCİ BULGU — İKİ MERKEZ VAR (1. aşamanın sebebi)

Şablon kanonik sanılıyor, değil:

| Ne | Nerede | Durum |
|---|---|---|
| `_universal` 18 kural | şablon | kanonik olması gereken |
| `turkce_tanimlayici_denetimi.py` (406 satır) | **pusula** | şablonda **YOK** |
| `kod-sozcukleri.txt` (338 sözcük ak liste) | **pusula** | şablonda **YOK** |
| pusula'nın kendi kuralları (22) | pusula | şablondan **12 sapma** |
| `templates/.claude/hooks/` (10+ kanca) | şablon | projelere kopyalanıyor |

`bkm-magaza` kurallar için **pusula'yı** işaret ediyor, şablonu değil. Yani iki merkez yarışıyor.
**Tek merkez kurulmadan referans modeli anlamsızdır** — neyin referansı olduğu belirsiz olur.

### pusula'daki kapının neden olgun olduğu (terfi ederken korunacak)

Üç katmanlı:
1. **Türkçe harf taraması** (`ıİşŞğĞüÜöÖçÇ`) — kapalı küme, kaçış yok
2. **ASCII kara liste** — hızlı, açık mesaj; ama **eksikliği görünmez**
3. **AK LİSTE** (338 sözcük) — bildirilen her adın her sözcüğü dağarcıkta mı? Değilse **insana sorar**

📐 Kara listenin körlüğü orada **ölçülmüş**: ak liste kurulunca `LikeKacir` ve `CalistirAsync`
aylardır koddaydı ve kara liste onları **hiç görmemişti**. Yani "kara liste yeter" bir teori değil,
çürütülmüş bir iddiadır.

Ayrıca: yorumları ve dizeleri **bilerek** taramıyor (onlar Türkçe olmalı), ak liste okunamazsa
**KOŞAMADI** der — *"sessizce kara listeye düşmek YASAK, çünkü o hâlde kapı çalışıyor GÖRÜNÜR."*

⚠ **Ak liste aracın kendi dizinine göre bulunuyor** (`Path(__file__).resolve().parent /
"kod-sozcukleri.txt"`). Taşırken **ikisi birlikte** taşınmalı; proje-yerel ek sözlük mekanizması
YOK (eklenebilir: `BKM_SOZLUK_EK` gibi bir ortam değişkeni — karar verilmedi).

⚠ **Bugün pusula'nın ak listesine 5 İngilizce sözcük eklendi** (`Blanked`, `Deleted`, `Stop`,
`Retention`, `Timer`) — bkm-magaza'nın yeni İngilizce kodu için. Terfide bu satırlar da gelmeli.

---

## 5. KARAR — şablon bir ÇATI olur

| | Solum (runtime) | Şablon (geliştirme-zamanı) |
|---|---|---|
| İçerik | kimlik, yetki, CRUD, web kabuğu | kurallar, skill'ler, kancalar, kapılar |
| Tüketim | `ProjectReference`/NuGet — **referans** | **referans** (yol), kopya değil |
| Kırıcı değişiklik | derleme kırılır, hemen görünür | `durum.sh` gösterir |
| Geri akış | tüketici isteği (İSTEK-NN) | `harvest.sh --promote` |
| Sürüm | lockstep + paket CHANGELOG'u | VERSION + CHANGELOG |

**Dört ilke:**
1. **TEK MERKEZ** — `_universal` kanoniktir; pusula'daki olgun kapılar buraya terfi eder.
2. **KOPYA DEĞİL REFERANS** — tüketicinin `CLAUDE.md`'si kanonik yolu işaret eder ve geçerli
   kuralları sayar. `bootstrap` yeni projeye **işaretçi** kurar.
3. **YEREL KURAL MEŞRUDUR — gerekçesi dosyada yazılı olur** (`capacitor-kabuk.md` deseni).
4. **SAPMA ÖLÇÜLEBİLİR OLUR** — `bin/durum.sh`; ölçülmeyen yakınsama olmaz.

**Reddedilenler:** forklamak (hastalığın kendisi) · Solum'a taşımak (runtime NuGet, .NET-bağımlı
xunit kapıları, lockstep tren, 39 deponun çoğu Solum kullanmıyor) · yeni ortak depo
(`footprint-ladder` 6. basamak; şablon zaten var) · şimdi yeniden adlandırmak (darboğaz ad değil).

---

## 6. YAPILACAKLAR — sırayla, her aşama ölçülür

### Aşama 1 · TEK MERKEZ — **bu oturumun asıl işi**
- [ ] `bash bin/harvest.sh --scan` → `_universal adayi` / `stacks/* adayi` işaretlileri ayır.
- [ ] pusula'nın olgun kapılarını terfi et: `turkce_tanimlayici_denetimi.py` + `kod-sozcukleri.txt`.
      Hedef: `templates/.claude/hooks/` ya da yeni `templates/tools/` (**yer kararı verilmedi**).
      İkisi **birlikte** taşınmalı (§4'teki yol bağımlılığı).
- [ ] Çakışan sürüm varsa kanonik olan **ölçülerek** seçilsin (`harvest.sh --diff <dosya> <proje>`),
      tercihle değil.
- [ ] Şablonun kendi `_universal` kuralları ile pusula'nınkiler arasındaki 12 sapma tek tek
      sınıflansın: **bayat mı, bilerek mi**. Bu insan kararıdır.
- [ ] pusula kopyalarını bırakıp referansa geçsin. ⚠ **Önce kontrol et: pusula'da başka oturum
      çalışıyor olabilir** (bugün çalışıyordu).

### Aşama 2 · BOOTSTRAP — kopya kipi varsayılan olmaktan çıkar
- [ ] `bin/bootstrap.sh` + `bin/bootstrap.ps1` için **`--reference` kipi**: `_universal`
      dosyalarını kopyalamak yerine `CLAUDE.md`'ye işaretçi yazar.
      **Birebir örnek:** `D:\Dev\bkm-magaza\CLAUDE.md` (§"Oturum başı — ZORUNLU").
- [ ] `--copy` kipi kalsın ama **açıkça istensin** ve gerekçesi `CLAUDE.md`'ye yazılsın.
- [ ] `durum.sh` kopya kipini raporlamaya devam etsin (kapı değil, görünürlük).

### Aşama 3 · DEPO DEPO GÖÇ — **bu oturumda YAPILMAZ**
- [ ] Her depo **kendi oturumunda**: kopyalar silinir, `CLAUDE.md` işaretçiye döner, yerel
      kurallar **gerekçeleriyle** kalır.
- [ ] 158 sapmanın her biri için *bilerek mi bayat mı* kararı — makine ayıramaz.
- [ ] Sıra önerisi: en çok sapan ve en çok tüketilen önce (`ajan` 16, `Operax` 15, `hal` 14,
      `BkmArgus` 13, `yonbkm` 13).
- [ ] ⚠ Bazı depolarda aktif oturum var; göç öncesi haber verilmeli (Solum'un `tuketici-haberi.md`
      deseni: kırıcı dokunuştan **önce** tek satır haber; orada **beş kez ihlal edilmiş**).

### Aşama 4 · Tek yön kalır
- [ ] Sapma sıfırlanınca `update-all` gereksizleşir; `harvest` yalnız "yerelde olgunlaşan → merkez"
      yönünde kalır.

---

## 7. TUZAKLAR — bugün ölçüldü, tekrar keşfetme

| Tuzak | Ne olur | Çözüm |
|---|---|---|
| **Git Bash heredoc `\\` → `\`** | UNC yolu (`\\BT-FIKRI\…`) tek ters bölüye iner; dosya sessizce bozulur. **Bugün iki kez vurdu** | Ters bölü içeren dosyayı heredoc'la yazma — Write aracı ya da PowerShell dosya işlemi |
| **PS 5.1 + BOM'suz UTF-8** | Türkçe yorumlu `.ps1` ANSI okunur, `—` dizeyi kapatır → parse hatası | `.ps1` **UTF-8 BOM'lu** kaydedilir |
| **`sed -i` paylaşımda** | SMB üstünde bozuk adlı artık dosya bırakır, silinemez | `sed -i` kullanma; Edit aracı |
| **Yanlış payda** | `grep -rl` `bin/`·`obj/`·`node_modules` sayar, `find` saymaz → oran uydurma çıkar. **Bugün iki kez yapıldı** | Aynı dosya kümesiyle ölç, paydayı yazdır |
| **Kapı yazıp sınamamak** | **Bugün iki kapı yarım yakaladı** (biri sınıf adını görüp metodu kaçırdı) ve ancak sabotajla görüldü | Kapı yazınca beklenen değeri **boz**, kırmızı gör, geri al |
| **Yanlış pozitifli kapı** | Bastırma öğretir, kural ikinci kez ölür. Bugün bir kapı **7 bulgunun 6'sı yanlış pozitif** çıktığı için **silindi** | Kapıyı mevcut depoda ölç; oran kötüyse kurma |
| **UNC + git** | "dubious ownership" | `git config --global --add safe.directory '%(prefix)///BT-FIKRI/Dev/<depo>'` |
| **Ev stili karışık** | `bin/*.sh` **ASCII Türkçe** (`kesfet`, `calistirmadi`), `README`/`docs` **düzgün Türkçe** | `coding-discipline`: mevcut stili taklit et — hangi dosyaya dokunuyorsan onunkini |

---

## 8. KARAR VERİLMEMİŞ — GMY'ye sorulacak

1. ~~**Depo adı.**~~ **KARAR VERİLDİ (GMY, 23.09): `Norma`** — Aşama 2 ile **aynı commit'te**.
   Gerekçe GMY'den geldi ve öneriden (`usul`) güçlüydü: **Solum da Latince.** Bu bir addan fazlası,
   bir **konvansiyon** kuruyor:
   | Dil | Katman | Örnek |
   |---|---|---|
   | **Latince** | altyapı / çatı | **Solum** (zemin) · **Norma** (gönye) |
   | **Türkçe** | ürün / iş | pusula · mizan · odak · tamim · bkm-magaza |
   Ad, hangi katmanda olduğunu kendisi söyler. Anlam eşleşmesi de tam: ikisi de yapı ustasının
   sözcüğü — zemini *solum* verir, şakülünde olduğunu *norma* söyler.
   ⚠ **Zamanlama şart:** depo bugün hâlâ kopyalıyor. Adı şimdi değiştirip davranışı bırakmak
   kozmetiktir ve "düzelttik" hissi verir — bu ekosistemin en pahalı hata sınıfı (§9). Ad,
   `bootstrap --reference` ile birlikte gelir.
   Elenenler: `usul` (Türkçe — anlamı doğru ama Latin/Türkçe katman ayrımını kuramıyor) ·
   `mihenk` (yalnız sınama tarafını anlatıyor, kural+skill+kanca yükünü taşımıyor) ·
   `kanon`/`nizam`/`esas`/`zanaat`/`ayar`/`pergel` (ağır, kolokyal ya da pusula ile karışıyor).
   Çakışma kontrolü: `D:\Dev` altında `Norma` **yok**.
2. **Kanonik kural kümesi.** Şablon 18, pusula 22, `ajan` 33 kural taşıyor. Üst küme hangisi?
   Fazlalıklar terfi mi edecek, yerel mi kalacak?
3. **Yerel kural eşiği.** Kaç depoda görülen kural `_universal`'a terfi eder? `harvest` bugün 5+
   için `_universal adayi`, 2+ için `stacks/*` diyor — **bu eşik doğrulanmadı**.
4. **Kapıların yeri.** `templates/.claude/hooks/` mi, yeni `templates/tools/` mü? (Python kapıları
   kanca değil araç; ayrı dizin daha doğru olabilir.)
5. **Proje-yerel ek sözlük.** Ak listeye her tüketicinin kendi İngilizce sözcüğünü eklemesi ortak
   dosyayı şişirir. `BKM_SOZLUK_EK` gibi bir ek dosya mekanizması kurulsun mu?

---

## 9. BU KARARIN KENDİ KAPISI YOK

Karar yazıldı, ama yarın bir depo yine kopya alabilir. `durum.sh` **raporlar, engellemez**.
Engelleme Aşama 2'de `bootstrap`'a girer.

📐 O güne kadar bu belge *yazılı ama zorlanmayan* bir kuraldır — ve **bu ekosistemin en pahalı
hata sınıfı tam olarak budur.** `bkm-magaza`'da bugün üç kez ölçüldü:
- **B-15** — kurulum betiği çalışan şemadan ayrışmıştı; kurulum yeşil, davranış kırık.
- **D-008** — KVKK saklama kuralı betikte **yorum** olarak duruyordu, koşan hiçbir şey yoktu;
  süre kâğıtta 90 gün, sahada **sonsuz**.
- **D-010** — `file-size-discipline` üç gün boyunca sessizce kaydı, çünkü kapı boyuta bakmıyordu.

Üçünde de kural **yazılıydı ve okunmuştu**. Kapısı yoktu.

---

## 10. İLGİLİ DOSYALAR

| Dosya | Ne |
|---|---|
| `docs/MIMARI-KARARI-2026-09-23.md` | karar, gerekçe, reddedilen seçenekler |
| `bin/durum.sh` | ekosistem ölçümü (AYNI/SAPMA/YEREL/REFERANS) — bu işle eklendi |
| `bin/harvest.sh` | geri akış: `--scan` · `--diff` · `--promote` |
| `bin/update-all.sh` | merkez → proje (kopya modeli; Aşama 2'den sonra gereksizleşir) |
| `bin/bootstrap.sh|ps1` | yeni proje kurulumu (Aşama 2'de `--reference` eklenecek) |
| `bin/projects.txt` | bugün **3 proje**; boşsa `durum.sh`/`harvest` otomatik keşfediyor |
| `CHANGELOG.md` 1.4.0 | önceki teşhis — doğru ama bir basamak eksik |
| `D:\Dev\bkm-magaza\CLAUDE.md` | **referans uygulama**: 0 kopya, 1 gerekçeli yerel kural |
| `D:\Dev\pusula\tools\turkce_tanimlayici_denetimi.py` | terfi edecek olgun kapı (§4) |
| `D:\Dev\Solum\.claude\rules\isimlendirme.md` | ak liste > kara liste gerekçesi, ölçümüyle |
